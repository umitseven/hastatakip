@model HastaTakipWeb.Models.Siniflar.TopImage

@{
    ViewBag.Title = "TopImageGetir2";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<br />

@using (Html.BeginForm("TopImageGüncelle2", "admin", FormMethod.Post))
{
    @Html.LabelFor(x => x.id)
    @Html.TextBoxFor(x => x.id, new { @class = "form-control", @readonly = "readonly" })
    <br />
    @Html.LabelFor(x => x.resim2,"Resim URL")
    @Html.TextBoxFor(x => x.resim2, new { @class = "form-control" })
    <br />
    <button class="btn btn-success">Güncelle</button>
}
