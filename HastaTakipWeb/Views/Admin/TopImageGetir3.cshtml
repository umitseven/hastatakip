@model HastaTakipWeb.Models.Siniflar.TopImage

@{
    ViewBag.Title = "TopImageGetir3";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<br />

@using (Html.BeginForm("TopImageGüncelle3", "admin", FormMethod.Post))
{
    @Html.LabelFor(x => x.id)
    @Html.TextBoxFor(x => x.id, new { @class = "form-control", @readonly = "readonly" })
    <br />
    @Html.LabelFor(x => x.resim3,"Resim URL")
    @Html.TextBoxFor(x => x.resim3, new { @class = "form-control" })
    <br />
    <button class="btn btn-success">Güncelle</button>
}
